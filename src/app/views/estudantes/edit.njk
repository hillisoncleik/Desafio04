{% extends "layout.njk" %}

{% block content %}
<div class="card">

    <section 
        class="imagem" 
        style="background: url({{ estudante.avatar_url }}) no-repeat center center/ cover">
    </section>
    <section class=dados>
        <form method="POST" action="/estudantes?_method=PUT">
            <h2>Editar Aluno</h2>

            {% include 'estudantes/fields.njk' %}
            

            <input type="hidden" name="id" value="{{estudante.id}}">
        </form>
    </section>
</div>

{% endblock content %}